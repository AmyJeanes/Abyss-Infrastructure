{{- if .Values.vpn.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: qbittorrent
type: Opaque
stringData:
  openvpn-user: {{ required "openvpn.user is required when vpn.enabled is true" .Values.openvpn.user | quote }}
{{- end }}
